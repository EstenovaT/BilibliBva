════════════════════════════════════════════════════════════
  Cloudflare Tunnel + Python 后端 部署方案（稳定方案）
  —— 解决 Cloudflare Worker 出口 IP 被 B 站风控（-412）问题
════════════════════════════════════════════════════════════

一、为什么需要本方案
────────────────────────────────────────────────────────────
  Cloudflare Worker 的请求从 Cloudflare 境外边缘 IP 出站，B 站
  对该类 IP 风控严格（-412），导致在线解析失败。
  Python 后端运行在你自己（国内）的机器/服务器上，请求从国内
  IP 出站，基本不会被风控；Cloudflare 只负责隧道、HTTPS、域名。

二、架构
────────────────────────────────────────────────────────────
  用户浏览器 → https://bva.estenova.top
             → Cloudflare 边缘（HTTPS 自动）
             → Cloudflare Tunnel（cloudflared，无需开端口/公网 IP）
             → 本机 http://127.0.0.1:8080（Python resolve_server.py）
             → Python 后端 → B 站 API（国内 IP 出站，无风控）

三、环境要求
────────────────────────────────────────────────────────────
  - 一台能长期在线的机器（家用电脑保持开机 / 云服务器均可）
  - Python 3.7+（本仓库 Python 后端零第三方依赖）
  - bva.estenova.top 域名已在 Cloudflare 托管（添加站点、改 Nameserver）
  - cloudflared 客户端

三-2、国内云服务器方案（长期运行推荐）
────────────────────────────────────────────────────────────
  优势：24 小时在线、国内 IP 出站、不占家用电脑。
  一键部署脚本（在服务器上执行）：
      sudo bash deploy/tunnel/setup_vps.sh [域名]
  脚本自动完成：安装 cloudflared → 拉取代码 → systemd 启动后端 →
  登录/创建隧道 → 生成 config.yml → 绑定域名 → 开机自启。
  说明：
  - 服务器若无法访问 GitHub 克隆（国内网络），用浏览器下载仓库 ZIP
    上传解压到 /opt/biliblivBva 后重跑脚本即可
  - 域名解析走 Cloudflare（隧道为出站连接），服务器无需开放公网端口；
    国内服务器对外提供 Web 服务正规要求 ICP 备案，个人小工具请自行评估
  - 可选：编辑 /etc/bili-resolve.env 配置 BILI_COOKIE 后重启服务
      systemctl restart bili-resolve

四、部署步骤（Windows 示例；Linux 命令类似）
────────────────────────────────────────────────────────────
  0. 一键配置（推荐，Windows）：
       deploy/tunnel/setup_tunnel.ps1
       自动完成：检查/安装 cloudflared → login → 创建隧道 →
       生成 config.yml → 绑定域名；随后运行
       deploy/tunnel/start_tunnel.bat 一键启动（后端+隧道）。

      「快速体验模式」（无需域名）：deploy/tunnel/quick_tunnel.bat
      直接生成临时公网地址 https://xxx.trycloudflare.com，先验证方案可用。

  1. 安装 cloudflared：
       winget install cloudflare.cloudflared
       （或下载 https://github.com/cloudflare/cloudflared/releases
         放到任意目录，命令用 .\cloudflared.exe 代替 cloudflared）

  2. 登录 Cloudflare 账号：
       cloudflared tunnel login
       （自动打开浏览器授权，会在本机生成凭据）

  3. 创建命名隧道：
       cloudflared tunnel create bva
       （记住输出的 Tunnel ID，如 1a2b3c4d-...）

  4. 编写隧道配置 C:\Users\<你>\.cloudflared\config.yml：
       tunnel: 1a2b3c4d-....（填你的 Tunnel ID）
       credentials-file: C:\Users\<你>\.cloudflared\<TunnelID>.json

       ingress:
         - hostname: bva.estenova.top
           service: http://localhost:8080
         - service: http_status:404

       （仓库已提供示例：deploy/tunnel/config.example.yml，复制后替换占位符即可）

  5. 绑定域名到隧道（DNS 记录，自动添加）：
       cloudflared tunnel route dns bva bva.estenova.top

  6. 启动 Python 后端：
       cd E:\project\aiagent\biliblivBva
       python src/script/start_local.py
       （服务跑在 127.0.0.1:8080，代理播放默认开启，无时长限制）
       可选：设置 B 站会话 Cookie 降低风控概率（推荐 BILI_COOKIE 整段）：
         Windows:  set BILI_COOKIE=完整Cookie字符串
         Linux:    export BILI_COOKIE=完整Cookie字符串
       获取方式：浏览器登录 bilibili.com 后，F12 → 应用 → Cookie →
       复制全部 Cookie 字符串（含 buvid3/buvid4/bili_ticket/SESSDATA）。
       ⚠️ Cookie 是账号凭证，仅放环境变量，勿写入代码/仓库/泄露

  7. 启动隧道：
       cloudflared tunnel run bva
       看到 "Registered tunnel connection" 即成功
       （仓库已提供 Windows 一键启动脚本：deploy/tunnel/start_tunnel.bat）

  8. 验证：
       浏览器访问 https://bva.estenova.top/
       解析一个视频，正常返回信息与直链

五、开机自启（可选）
────────────────────────────────────────────────────────────
  Windows（服务方式）：
    cloudflared service install
    （需先配置好 config.yml；卸载：cloudflared service uninstall）
  或使用 NSSM 把 cloudflared 和 python start_local.py 都注册为服务。
  注意：Windows 服务方式默认以 SYSTEM 运行，路径权限需确认。

  Linux（systemd，云服务器）：
    仓库已提供后端服务单元：deploy/tunnel/bili-resolve.service
    （复制到 /etc/systemd/system/，可选在 /etc/bili-resolve.env
     配置 BILI_PROXY / BILI_COOKIE）
    sudo systemctl daemon-reload
    sudo systemctl enable --now bili-resolve
    sudo systemctl status bili-resolve
    隧道开机自启：
    sudo cloudflared service install
    （需先按上文把 config.yml 放到 /etc/cloudflared/）
    启动/查看状态：
      sudo systemctl enable --now cloudflared
      sudo systemctl status cloudflared

六、对比 Worker 版
────────────────────────────────────────────────────────────
  ✅ 解析稳定：国内 IP 出站，无 -412
  ✅ 代理播放无 30 秒限制，1080P 网页内可播
  ✅ /ex 完全版入口同样可用（Python 后端已支持）
  ⚠️ 需要一台常开机器；网页、域名、API 行为与 Worker 版完全一致
  ⚠️ 流量走你的机器/服务器带宽，注意用量

  说明：Cloudflare Worker 无法把出口 IP 改为国内（Cloudflare 中国网络
  仅企业版开放且需 ICP 备案），所以"国内 IP 出站"只能由你的机器/服务器
  完成，隧道仅负责把公网请求转发回来。

七、备注
────────────────────────────────────────────────────────────
  - 若不想改域名，也可先访问隧道提供的临时域名体验
    （cloudflared tunnel --url http://localhost:8080，随机 *.trycloudflare.com）
  - 此方案与 Worker 版互不冲突：Worker 仍可保留（配 SESSDATA 后
    可能也能解析），两条路可同时在线，域名任选其一
  - Worker 版代码中的 SESSDATA 登录功能保留（env.SESSDATA）；
    Python 版对应环境变量为 BILI_SESSDATA
════════════════════════════════════════════════════════════
